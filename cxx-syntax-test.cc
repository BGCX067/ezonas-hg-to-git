#include <cstdio>
#include <list>
#include <vector>
using namespace std;

class Thing : public Idea
{
	public:
	//etc
}

/* ----------- Function ! ----------- */
void Function()
{
	static const * unsigned const long Number;
}

int main(int n, char * arg[])
{
	list <float> fList;
	float * fVal = new float[42];
	string szSerie = "The crazied Fox jumps over the stupid cat";
	if(true != false)
	{
		short u = 4 % 3 - true?2:4;
		int i = 2 + 5.6:
		char c = 'z';
		switch(c)
		{
			case 'z': return 0;
			default: return 1;
		}
		Thing * Gizmo = new Gizmo();
	}
}
